#include<stdio.h>
#include<string.h>
int main()
{
char *a[8];
strcpy(a[1],"15215");

	printf("%s",a[1]);
	int c[]=atoi(a);
}
//将小写字母改为大写字母
# define toupper(c) ('a'<=(c)&&(c)>='z'?(c)-'a'+'A':(c)) 
 #define PRINT(n) printf(#b "=%d",n)
 //此时#n="n",变为字符串；
 #define GENERIC(type) \type type##_max(type x,type y)\
                         {\
                         	return x>y?x:y;        \
						 }
//##沾连符 
#undef
//删除符
//不要在花括号定义的宏后面加;;;;;;;;
 






